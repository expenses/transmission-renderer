[package]
name = "shared-structs"
version = "0.1.0"
edition = "2021"

[dependencies]
glam = { version = "0.20", features = ["libm"], default-features = false }
num-traits = { version = "0.2.14", default-features = false }
